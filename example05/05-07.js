// 리터럴 표현식
10
"Hello"

// 식별자 표현식 (선언이 이미 존재한다고 가정)
sum
person.name
arr[1]

// 연산자 표현식
10 + 20
sum = 10
sum !== 10

// 함수/메소드 호출 표현식(선언이 이미 존재한다고 가정)
square()
person.getName()

/*
    표현식은 값으로 평가되며, 표현식과 표현식이 평가된 값은 동등한 관계, 즉 동치(equivalent)다.
    자바스크립트 표현식 1 + 2는 평가되어 값 3을 생성하므로 1 + 2와 값 3은 동치다.
*/