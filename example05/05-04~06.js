var score1 = 100;

var score2 = 50 + 50;

var score3;

/*
    score1 - 자바스크립트 엔진에 의해 평가되어 값을 생성하므로 리터럴은 그 자체로 표현식이다.
    score2 - 50 + 50은 리터럴과 연산자로 이뤄져 있는데, 50 + 50도 평가되어 숫자 값 100을 생성하므로 표현식이다.
    score3 - 변수 식별자를 참조하면 변수 값으로 평가된다. 식별자 참조는 값을 생성하지는 않지만 값으로 평가되므로 표현식이다.

    표현식은 리터럴, 식별자, 연산자, 함수 호출 등의 조합으로 이뤄질 수 있다.
    값으로 평가될 수 있는 문은 모두 표현식이다.
*/